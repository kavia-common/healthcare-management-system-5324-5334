# Copy this file to `.env` and adjust values for your environment.
# Backend base URL used by the Flutter frontend.
# For local Android emulator, prefer http://10.0.2.2:3001
# For iOS simulator or desktop, you can use http://localhost:3001
# For preview deployments, point to the backend preview URL (port 3001 by default).
BACKEND_BASE_URL=http://10.0.2.2:3001
